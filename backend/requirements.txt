# WizCoCo Flask API - 상담사/내담자 API
Flask>=3.0.0
flask-cors>=4.0.0
firebase-admin>=6.4.0
# google-cloud-firestore==2.29.0 (2026-08-21 릴리스, "update googleapis and regenerate")
# 부터 "(default)" 데이터베이스 경로 생성이 깨져 모든 Firestore 호출이
# InvalidArgument: 400 Invalid database id (default) 로 실패함.
# 이전 안정 버전(2.28.1)으로 고정.
google-cloud-firestore==2.28.1
bcrypt>=4.1.0
python-dotenv>=1.0.0
gunicorn>=21.0.0
